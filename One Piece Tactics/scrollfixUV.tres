[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform vec2 d = vec2(1,0);
uniform float s = 0.1;
uniform float scale = 1.0;
uniform vec2 offset = vec2(0,0);

void fragment(){
	COLOR = texture(TEXTURE, SCREEN_UV*scale + (d*TIME*s) + offset);
}"
